export interface Int_AuthResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  UserID: string;
  Name: string;
  Mobile: string;
  Email: string;
  issued: string;
  expires: string;
}
